import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taf.gifexplorer',
  appName: 'GifExplorer',
  webDir: 'dist/frontend-prueba-tecnica/browser',

};

export default config;
